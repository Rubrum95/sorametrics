# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Local config & secrets
.env
.env.local
.env.*.local

# Databases (production state, not for git)
*.db
*.db-shm
*.db-wal

# PM2 logs & state
*.log
.pm2/

# Backups (manual snapshots — both .bak.* and .bak-* variants)
*.bak
*.bak.*
*.bak-*
*.tmp
*.swp

# Backfill state files (regenerated on each run)
backfill_state.json
backfill_orderbook_state.json

# Media assets (large files, deployed separately via SCP)
music/
news/

# Runtime state
.squid_watchdog_state

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Project-private (not for public repo)
CLAUDE.md
tasks/
.claude/
.vps-snapshot/

# Local working copies (not part of this repo)
sorametrics v6/
sorametrics-v33 (proyecto sorametrics rust)/
